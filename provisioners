provisioners "local-exec" {
    command = templatefile("linux-ssh-config.tpl", {
        hostname = self.public_ip,
        user = "ubuntu",
        identifyfile = "~/.ssh/terrkey"
    })
    interpreter = ["bash", "-c"]
}
